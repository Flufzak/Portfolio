---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'div'> {}

const { class: className, ...attributes } = Astro.props;
---

<div
  {...attributes}
  class:list={['h-px bg-(--border)', className]}
  aria-hidden="true"
></div>
